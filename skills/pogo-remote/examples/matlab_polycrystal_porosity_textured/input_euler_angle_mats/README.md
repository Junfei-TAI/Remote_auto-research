# input_euler_angle_mats

Place `.mat` files here that contain generated or processed Euler-angle sets,
for example a variable such as:

- `euler_angles_final_deg`

Used by `Assigning_EA_2_EG_models.m`.
